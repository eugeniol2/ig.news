import styled from './styles.module.scss';
export function SubscribeButton() {
    return (
        <button className={styled.signInButtonContent}>
            Subscribe now
        </button>
    );
}